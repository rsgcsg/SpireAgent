[project]
name = "sts2-mcp"
version = "0.3.0"
description = "Thin MCP adapter for the state-bound STS2 Agent Bridge v2"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.7.1",
    "httpx>=0.25.0",
]

[project.scripts]
sts2-mcp = "server:main"
