[project]
name = "sts2-mcp"
version = "1.0.0"
description = "Thin MCP adapter for the STS2 Human-Equivalent Connector"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.7.1",
    "httpx>=0.25.0",
]

[project.scripts]
sts2-mcp = "server:main"
