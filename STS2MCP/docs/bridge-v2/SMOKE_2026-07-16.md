# Deck Enchant Bridge v2 Smoke - 2026-07-16

## Identity

- Bridge: `0.5.0-dev`
- Protocol: `2.0-preview.1`
- Game: `v0.108.0`
- Game commit: `58694f64`
- Main assembly hash: `-2044609792`
- Surface: `NDeckEnchantSelectScreen`
- Organic source: saved `Wood Carvings` event, visible `Snake` option

No fixture, console command, hidden game state, or arbitrary execution payload
was used.

## Observation Result

The selecting state reported:

- `readiness=ready`;
- enchantment `SLITHER`, title `Slither`, visible dynamic description, amount 1;
- `min_select=1`, `max_select=1`, selected count 0;
- 13 distinct card instance IDs;
- 13 opaque `toggle_card` legal actions;
- no missing completeness fields.

The old v1 response at the same screen had exposed only cards and
`can_confirm=true`; it did not expose the enchantment or selection stage.

## Command Result

| Check | Result | Evidence |
|---|---|---|
| deliberately stale submit | rejected before UI action | `stale_state` |
| first card select | completed | `selected_card_membership_changed` |
| identical request retry | returned same completed record | no second action start |
| preview state | correct | selected count 1; confirm/cancel only |
| cancel preview | completed | `preview_closed_and_selection_cleared` |
| second card select | completed | `selected_card_membership_changed` |
| final confirm | completed | `enchantment_screen_closed_after_preview_confirm` |
| post-action v1 event | advanced to visible `Proceed` | event continuation completed |
| post-action v2 state | unsupported, no actions | next surface intentionally absent |

There were no command failures, timeouts, unknown outcomes, mod initialization
errors, or generic-confirm fallbacks.

## Security Result

- No-Origin local HTTP clients succeeded.
- `Origin: https://example.com` was rejected with HTTP 403.
- `Origin: http://localhost:3000` was accepted and echoed.

## Residual Limitation

Bridge v2 does not yet implement a deck inspection surface after the selection
screen closes. The smoke proves the game's selection task continued to the event
`Proceed` state; it does not expose a post-close v2 card-instance snapshot for
independent enchantment reinspection. That is future surface work, not a reason
to leak the deck through an unsupported response.
