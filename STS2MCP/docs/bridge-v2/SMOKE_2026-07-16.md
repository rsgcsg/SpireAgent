# Deck Enchant Bridge v2 Smoke - 2026-07-16

## Identity

- Bridge: `0.5.0-dev`
- Protocol: `2.0-preview.1`
- Game: `v0.108.0`
- Game commit: `58694f64`
- Main assembly hash: `-2044609792`
- Surface: `NDeckEnchantSelectScreen`
- Organic source: saved `Wood Carvings` event, visible `Snake` option

No fixture, console command, hidden game state, or arbitrary execution payload
was used.

## Observation Result

The selecting state reported:

- `readiness=ready`;
- enchantment `SLITHER`, title `Slither`, visible dynamic description, amount 1;
- `min_select=1`, `max_select=1`, selected count 0;
- 13 distinct card instance IDs;
- 13 opaque `toggle_card` legal actions;
- no missing completeness fields.

The old v1 response at the same screen had exposed only cards and
`can_confirm=true`; it did not expose the enchantment or selection stage.

## Command Result

| Check | Result | Evidence |
|---|---|---|
| deliberately stale submit | rejected before UI action | `stale_state` |
| first card select | completed | `selected_card_membership_changed` |
| identical request retry | returned same completed record | no second action start |
| preview state | correct | selected count 1; confirm/cancel only |
| cancel preview | completed | `preview_closed_and_selection_cleared` |
| second card select | completed | `selected_card_membership_changed` |
| final confirm | completed | `enchantment_screen_closed_after_preview_confirm` |
| post-action v1 event | advanced to visible `Proceed` | event continuation completed |
| post-action v2 state | unsupported, no actions | next surface intentionally absent |

There were no command failures, timeouts, unknown outcomes, mod initialization
errors, or generic-confirm fallbacks.

## Security Result

- No-Origin local HTTP clients succeeded.
- `Origin: https://example.com` was rejected with HTTP 403.
- `Origin: http://localhost:3000` was accepted and echoed.

## Residual Limitation

Bridge v2 does not yet implement a deck inspection surface after the selection
screen closes. The smoke proves the game's selection task continued to the event
`Proceed` state; it does not expose a post-close v2 card-instance snapshot for
independent enchantment reinspection. That is future surface work, not a reason
to leak the deck through an unsupported response.

## Re-SpireAgent Organic Lifecycle

The rebuilt client subsequently exercised the exact Bridge slice through its
normal prompt, validation, execution, and recording path:

- dry run: `run-20260716091118-5lc1m6`;
- select run: `run-20260716091134-so8fxw`;
- preview-confirm run: `run-20260716091148-45enri`.

DeepSeek returned strict JSON with `finishReason=stop`, selected only advertised
opaque action IDs, and the client observed `completed/confirmed` with
`selected_card_membership_changed` followed by
`enchantment_screen_closed_after_preview_confirm`. No repair, retry, unknown
outcome, or v1 execution path was used. Post-close deck reinspection remains
unavailable, so this proves the action lifecycle rather than every persistent
deck fact after the overlay closes.

## Event Option Bridge v2 Smoke

### Identity

- Bridge protocol: `2.0-preview.2`
- Game identity: `v0.108.0|58694f64|-2044609792`
- Surface: ordinary `SUNKEN_STATUE` event option screen
- Re-SpireAgent run: `run-20260716102313-aav6cl`
- Decision: `decision-000001-mrnd27ry-ryqct9`

### Lifecycle Result

The Bridge reported separate `context.kind=event` and
`surface.kind=event_option`, `readiness=ready`, no missing completeness fields,
and two state-bound opaque actions: `Grab the Sword` and `Dive into the Water`.
Re-SpireAgent normalized the state with `actionAuthority=bridge_advertised`;
no v1 action reconstruction was mixed in.

DeepSeek returned one strict JSON selection for the advertised `Dive into the
Water` action. The local stale-state guard matched, the Bridge revalidated the
same action/state pair, and its lifecycle completed as `confirmed` with
`event_option_applied_or_required_subsurface_opened`. The client settled after
two polls. The observed post-state changed from 75 HP / 0 gold to 68 HP / 104
gold and exposed only the Bridge-advertised `Proceed` action, matching the
visible selected option.

This is organic execution evidence for the ordinary event-option vertical
slice. It is not strategic-quality evidence and does not qualify ancient
dialogue, arbitrary event overlays, or other surfaces.

## Combat Turn Bridge v2 Smoke

### Identity

- Bridge protocol: `2.0-preview.2`
- Game identity: `v0.108.0|58694f64|-2044609792`
- Surface: ordinary player-phase combat against one visible `Seapunk`
- Re-SpireAgent run: `run-20260716104703-gqj32m`
- Decision: `decision-000001-mrndwutq-b9glny`

### Lifecycle Result

The Bridge reported `context.kind=combat`, `surface.kind=combat_turn`, and
`actionAuthority=bridge_advertised` after Re-SpireAgent projection. It exposed
round/phase, the player's hand and resources, one enemy with an 11-damage
attack intent, and six state-bound opaque actions. The completeness contract
was action-complete for the immediate turn; pile contents were explicitly
omitted pending a future read-only inspection protocol.

DeepSeek selected the advertised targeted Strike action. The local stale-state
guard matched; Bridge revalidation and the normal game `CardModel.TryManualPlay`
path accepted the exact card/target pair. The command completed with
`card_left_hand_or_required_subsurface_opened`, then settled after two polls.
The observed post-state retained the same combat surface while consuming one
energy, moving the selected card out of hand, and reducing Seapunk from 44 to
38 HP. No v1 action reconstruction, retry, timeout, or unknown command outcome
was used.

This qualifies one immediate targeted-card lifecycle only. It does not qualify
potion use, end turn, self/friendly/multi-target cards, combat card-selection
overlays, every enemy intent, or longer phase transitions.
