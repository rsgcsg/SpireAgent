# Human-Equivalent Connector

This is the current game-side contract. Read [Protocol](PROTOCOL.md) and
[Coverage](COVERAGE.md). Connector V3 docs describe rollback/history only.

The Gateway exposes current player-visible UI facts and exact current native
affordances. It delivers one input and returns a successor. It does not require
or claim complete business transaction semantics.
