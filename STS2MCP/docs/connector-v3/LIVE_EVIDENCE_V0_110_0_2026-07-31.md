# Connector V3 v0.110.0 Live Evidence: 2026-07-31

This record attributes the first V3 journey on STS2 `v0.110.0`. Raw run
directories and provider output remain local and are not repository artifacts.

## Exact Runtime

- source revision: `8c68d124e7e02ffd4a8cf0a243a674e34fa92858`
- protocol: `3.0-preview.1`
- loaded SHA:
  `68eed0b4890a96741dcb3f234e936149bfcc32affc806eb0c17db1142cc69685`
- loaded MVID: `54decad4-0ab8-4b4a-92c7-04aa2b5a35fb`
- runtime: `a009ff9f1bbf4a35bffe5a1c73ae4e95`
- game: `v0.110.0`, commit `eecc8c4d`, runtime assembly hash
  `-119477798`, release-declared hash `368442230`
- Modset: `exact_bridge_only`,
  `c824ed0ba870868caadfe3331a21a8ae843348a9e46551f6f80f7c9a621c4656`
- permission: `migration_exploration`, encounter-scoped provisional trial
- durable qualification: none

The Gateway reported the same protocol, SHA and MVID as the built and installed
artifact. One canonical `STS2_MCP` manifest was present.

## Run Set

The exact runtime produced 21 local runs from
`run-20260731034327-fti52b` through `run-20260731040632-00yz1g`.
Together they recorded 140 decisions:

- 98 `executed_and_settled`;
- 18 `executed_checkpoint_pending`;
- one `executed_unsettled`;
- 19 `not_executed_invalid_state`;
- one `not_executed_non_actionable_state`;
- three `not_executed_stale_state`.

There were 117 submitted V3 commands. All used V3 parameterized candidates and
V3 receipts: 48 direct native resolver commands and 69 bounded
Provider-native migration commands. Receipts contained 116
`completed/confirmed` outcomes and one `unknown/unknown` outcome.

The run set exercised combat, event, map, reward, shop, rest, treasure,
card-reward, combat-hand selection, deck removal, deck enchant, game over,
main menu, single-player menu and character selection. The final run
`run-20260731040632-00yz1g` completed 19 decisions, returned to the normal
non-actionable main-menu boundary and recorded `completedGame=true`.

This is exact-runtime evidence that V3 observation, parameterized command,
receipt and one-game supervision were used. It is not evidence that every
observed family was executable or that the runtime has a durable
qualification.

## Failures And Attribution

### Map ABI drift

Sixteen invalid-state stops came from
`NMapDrawings.GetLocalDrawingMode()`. In v0.110.0 the CLR signature changed to
`GetLocalDrawingMode(bool useOverride = true)`. A DLL compiled against the old
zero-parameter method threw `MissingMethodException`; an optional source
parameter does not preserve the old CLR call signature.

The repaired source binds only the audited zero-parameter or one-boolean
signature and invokes the current form with `true`. It also moves map travel
and annotation controls to V3 direct native resolvers. This repair has source
and automated evidence only until a new artifact is cold-loaded.

### Dream Catcher rest completion

`run-20260731034655-c22gu4` submitted Heal at a rest site. Native healing
occurred, then Dream Catcher opened an exact card-reward child. The old Outcome
waited for parent progress and timed out, producing the only
`unknown/unknown` receipt.

The repaired Outcome accepts the exact minimum HP increase plus the exact
native `NCardRewardSelectionScreen` child handoff as action-local completion.
Rest controls now use V3 direct native resolvers. This repair is pending a new
exact-runtime action.

### Symbiote deck enchant source

`run-20260731035127-qn4mc8` reached the Symbiote event's Corrupted enchant
selector. The loaded Gateway correctly exposed a visible unsupported
interaction because the source was not yet registered. Re then incorrectly
projected that reduced unsupported payload as a full v2 deck-enchant surface
and failed strict decode on the absent `stage`.

Re now projects every V3 `execution_support=unsupported` interaction as a
typed unsupported surface. The Gateway separately adds an exact Symbiote
source contract: exact event task, Corrupted enchantment, one required card,
non-cancelable automatic commit, exact screen/card operands, and V3 direct
native resolvers. The repair does not generalize unknown enchant callers and
is pending exact-runtime mutation evidence.

### Expected fail-closed boundaries

- three stale choices were refused before execution and were not retried;
- Crystal Sphere remained typed unsupported;
- an unknown `NDeckCardSelectScreen` source remained typed unsupported;
- no hidden information was exposed and no V2 action ID entered execution.

## Evidence Boundary

The v0.110.0 journey qualifies only the exact loaded SHA/MVID/runtime above.
The subsequent repair was built and installed as SHA
`24f44c2482efe36a86be3dd9d085542676c275f5acf7f9c40b47329616069c2b`,
MVID `5feaf546-00c4-436c-8391-96a6087e8eb7`. It is not loaded and does not
inherit these action results.

Non-claims:

- no persistent V3 qualification or durable claim;
- no V3-native Inspection/detail journey;
- no repaired map, rest or Symbiote action evidence;
- no Kifuda, New Leaf, Crystal Sphere or generic deck-selector support claim;
- no cross-version or cross-Mod qualification.
