# Current Functional Roadmap

## V3-0: Vertical Source Cutover

Status: source complete at `3.0-preview.9`. Gateway/Re use V3 by default; REST
and MCP are thin transports. Re has no V2 projection sidecar, and Connector V3
has no Provider draft or legacy-binding execution path.

## V3-1: Exact Runtime Canary

Status: Preview.8 has archived exact loaded coverage on SHA `26c5baae...e529`,
MVID `d1476ec1-4da8-4658-b594-6fea40de3ca0`, but unrecorded provenance. The
installed Preview.9 artifact has SHA `540acf96...d49` and MVID
`afa5d986-d82d-4a01-b2ab-5509e3926f61`; it has not been cold-loaded.

Exit: verify exact loaded identity, exercise one final selector and targetless
potion, retain receipts, and keep the authority runtime-scoped.

## V3-2: Ordinary Journey

Status: multiple exact v0.110.1 journeys exist, including Preview.7's 202
settled commands and the archived Preview.8 completed-game run. Their
provenance and exact tuples remain historical coverage only. Preview.9 needs a
same-artifact journey before any runtime completion claim.

## V3-3: Native Family Migration

Status: all 94 manifest operations have explicit native contracts. Combat
pile, deck transform and Wood Carvings are now typed direct V3 slices. Provider
action publication, migration fallback authority and Re V2-shaped sidecar
counts are all zero.

Remaining work is runtime qualification, not another source migration wave:

1. targetless potion and final selector exact-runtime canaries;
2. one same-artifact ordinary journey;
3. classify unsupported sources without widening a Surface or operation.

## V3-4: Visibility And Human Information

Status: persistent summary, complete current Surface, typed state-bound
Inspection and bounded `surface_card` linked detail are implemented. Preview.5
proved current/stale run-deck and linked-detail behavior. Combat-piles and
shop-catalog exact Preview.9 evidence remain pending.

Semantic accessibility stays the default. Physically opening native pages is
an optional human-equivalence evidence profile and never mutation authority.

## V3-5: Authority And V2 Retirement

Status: source closure reached: `94` explicit contracts, `0` fallback
contracts, `0` persistent fallback claim admission and no direct-family
Provider draft publication. Empty scopes remain Fail Closed. V2 endpoints may
remain mounted only for rollback and migration diagnostics; Re and V3 mutation
do not consume them.

Exit: cold-load/rollback evidence, durable qualification lifecycle tests,
formal archive of the V2 production boundary, and a clean same-artifact
journey. Do not enlarge authority to make the evidence easier.

## Later

After Live closure, resume measurable Agent evaluation. Headless and learning
remain separate future projects.
